'use strict';

/**
 * estabelecimento controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::estabelecimento.estabelecimento');
